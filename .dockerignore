# Dependencies installed in container
vendor/

# Frontend lives in its own folder; backend image is PHP only
frontend/

# Git and IDE
.git
.gitignore
.vscode

# Local / sensitive (never in image)
.env
config.local.php
api/webhook/nola-sms-pro-*.json
*.log
api/webhook/sms_log.txt
api/webhook/last_payload.json

# Debug/scratch files — never deploy these
raw_logs.txt
raw_logs_utf8.txt
scratch/
oauth_debug.php
debug_*.php
tmp_*.php
scratch_*.php
test_*.php
generate-test-link.php
migrate_*.php
seed_*.php
outputs/
*.exe

# SSL cert bundle (not needed inside container)
cacert.pem

# Planning/design docs
docs/
